"""
This is a Python script that serves as an entry point for the Atlas Talk.

The `main` function is imported from `atlas_talk.cli`, which represents the
core functionality of the Atlas command-line interface. When this script is run,
it calls the `main` function, allowing the user to interact with the Atlas
interface.
"""

from atlas_talk.cli import main

if __name__ == "__main__":
    main()
